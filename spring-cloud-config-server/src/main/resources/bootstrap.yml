spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wm-hao/spring-cloud-config
          search-paths: repos
  application:
    name: cloud-config-server
server:
  port: 8000